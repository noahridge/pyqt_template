"""Core infrastructure: fault handling, safe slots, workers, config, signals.

Nothing in :mod:`hmi.core` imports from views/services, so it stays the stable
foundation the rest of the app depends on.
"""
